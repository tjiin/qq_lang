
factorial(x) => {
    switch (x) {
        case 0:
            let ret = 1;
        default:
            let ret = x*factorial(x-1);
    }
    return ret;
}

factorial(10);


/*
( func(x) => x + 1; )(0)
2 + func(1);

let f = g(x) => x*2;
f(-1);

f() => {let x = 2; return x}
f();

def f() {
    y = 7;
    return(x + 1);
};
let x = 2;
f();
x + 2;
*/
